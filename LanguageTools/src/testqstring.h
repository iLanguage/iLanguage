	class TestQString: public QObject
	{
	public:
		Q_OBJECT
	private slots:
	    // the test function have to be declared as a private slot so the test framework finds it and can execute it.
	   // void toUpper();


	};

