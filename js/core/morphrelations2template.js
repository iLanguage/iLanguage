#! /usr/bin/env node
console.log(process.argv);
var userArgs = process.argv.slice(2);


echo "
    Use a Hidden Markof Model to generalize from precedence relations to a finite state transducer, use this for both encoding mophological boundaries for allomorphs, and for inserting new morphemes into the current fintie state transducer."
