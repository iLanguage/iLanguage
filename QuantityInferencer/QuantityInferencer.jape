Phase: GCJape
Input: pair h t 
Options: control=appelt

Rule: MYRULE
(
 {pair}
):rte
-->
{
	gate.AnnotationSet quality = (gate.AnnotationSet)bindings.get("Age");
	gate.Annotation qualityAnn = (gate.Annotation)quality.iterator().next();
	gate.FeatureMap features = Factory.newFeatureMap();

	//find out if the exact is unambiguous
	String exact = "hello";
	
	features.put("exact", exact);
	features.put("rule", "FirstName");
	outputAS.add(quality.firstNode(), quality.lastNode(), "Age",features);
}
