'use strict';

describe('lib/dialect', function() {

  it('should accept an ISO language code and/or a text to guess the dialect/language', function() {
    expect(true).toBeTruthy();
  });

  it('should be able to guess the stopword weights for this dialect', function() {
    expect(true).toBeTruthy();
  });

  it('should be able to guess the tokenization for this dialect', function() {
    expect(true).toBeTruthy();
  });

});
