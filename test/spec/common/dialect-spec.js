'use strict';

describe('lib/dialect', function() {

  xit('should accept an ISO language code and/or a text to guess the dialect/language', function() {
    expect(true).toBeTruthy();
  });

  xit('should be able to guess the stopword weights for this dialect', function() {
    expect(true).toBeTruthy();
  });

  xit('should be able to guess the tokenization for this dialect', function() {
    expect(true).toBeTruthy();
  });

});
